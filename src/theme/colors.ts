const pallette = {
  white: '#fff',
  black: '#000',
};

export const colors = {
  common: {
    background: pallette.white,
    shadowDefault: pallette.black,
  },
};

export type Colors = typeof colors;
